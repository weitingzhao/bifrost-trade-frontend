/** Shared class names for compact position tables (Stocks / Independent / Coverage). */
export const denseTable = {
  sectionTitle: 'text-sm font-semibold text-foreground mb-1',
  emptyHint: 'text-xs text-muted-foreground',
  sortableHead:
    'cursor-pointer select-none text-muted-foreground hover:text-foreground focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring',
} as const
