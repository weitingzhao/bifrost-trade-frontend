/** Strategy instance detail sidebar width — keep in sync with `--instance-detail-sidebar-width` in index.css */
export const INSTANCE_DETAIL_SIDEBAR_WIDTH_PX = 960

/** Max width when compare mode shows two panes side by side */
export const INSTANCE_COMPARE_MAX_WIDTH_PX = 1880

/** Viewport breakpoint: docked sidebar vs modal overlay */
export const INSTANCE_DETAIL_NARROW_MAX_PX = 960
