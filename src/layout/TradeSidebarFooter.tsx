/**
 * The sidebar foot is the user centre (design Rev .54): one row — the avatar
 * with its summary lamp, Operator, the degraded count, and the door into
 * System (or back to Trade). The System row and the Back-to-Trade row it
 * replaced are its door now; the Ops Console link is one of its entries.
 */
import { SidebarUserCenter } from './UserCenter'

export function TradeSidebarFooter() {
  return <SidebarUserCenter />
}
