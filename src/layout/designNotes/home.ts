/**
 * Design-walk notes · Home.
 *
 * Home — the layer organised by time.
 *
 * One file per layer because the record only grows: the notes are long on
 * purpose, and a single catalogue crossed the 800-line ratchet on the day the
 * Review re-walk was written. Split by the group each page belongs to, which
 * is the seam the reader already has in their head.
 */
export const HOME_NOTES: Record<string, string> = {
  '/market/live':
    'Walked 2026-09-22 against Market Live.dc.html at Rev 2026-09-20.16, rendered side by side with the app on local DEV (:5173). The prototype’s own subtitle says what it is — a redraw of the production page on the shared shell — so this walk is a comparison, not a rebuild: the production page is the Owner’s daily tape and its categories, filters, sort modes, account view and drag reordering are not in the design because the design is a sketch of the shape, not a list of what to keep. Three things were genuinely absent, and each of them was absent for a reason worth recording. First, the page had no header at all — not a PageHeader, not an h1 — which is both the design’s opening and the house rule in CLAUDE.md. A comment explained it: the Copilot button had been given its own row because there was no header to sit in. Built as the design has it: the title, the one line, and the three readings pinned right — the streams lamp with streams N/M, the orders-link lamp with the working count, and the session (AH · 18:25 ET). The session rule moved from pages/home/today/todayModel.ts to lib/marketSession.ts, because a page in pages/market may not import from pages/home and two pages disagreeing about whether it is still the closing auction is what a shared rule prevents (§14.2). Second, the whole page had zero links — measured, not estimated: forty-odd tickers, four panels, and not one anchor. The design’s last column is a per-row Dossier → / Chain → button; on a table already this wide the ticker itself is the link instead, which is the same destination without a tenth column, and the divergence is named here. A stock opens the Dossier, an option contract opens its underlying’s chain via ANALYZE_HUB.discovery (verified by clicking: /research/discovery?symbol=AMD resolves to /research/symbol?symbol=AMD&tab=chain). The Watching Options label was the worst of them — it already wore denseTableEntityLink and was a span, a dead end in a link’s clothes. Open Orders’ symbols link too, and that pane now carries the design’s read-only sentence: orders are placed in TWS, this view is read-only (D10). Third, the Reference panel — indices & vol — did not exist, and the reason is the finding: status.live_ui.reference_indices is this app’s own configuration point for exactly that panel and it answers [] on DEV, so the slot existed, the config was empty, and nothing ever rendered. The panel no longer waits for it — the design’s five are the default and a declared index is added rather than fetched and dropped. Measured on DEV 2026-09-22: GET /market/bars/benchmark answers for SPY, QQQ, IWM and TLT; VIX answers for nothing, under any spelling — VIX, ^VIX and VIX.IND all come back empty, /market/quotes has no row for it, and the research engine carries no VIX series. The feed subscribes to equities and ETFs and VIX is an index. VXX does answer and is deliberately not substituted: it is an ETN on VIX futures, a different quantity wearing the right name. The tile keeps its place, says so, and the panel header reads 4 of 5 reading · VIX unavailable. Two bugs were caught by rendering rather than by reading the diff, both mine, both in the first pass. The header read streams 27/23 — a fraction over one — because the numerator counted the quote map, which holds option contract keys as well as symbols, while the denominator counted symbols alone; countFreshQuotes now counts against what the page asked to stream and lives beside the lamp in utils/livePageLamps, so the lamp and the count answer to the same 60-second window. And the Reference tiles printed QQQ at 80.8% for one render: computeDailyChange returns percent units, not a fraction, and the panel multiplied by 100 again. Both have tests. Diverged, each with its reason. The design’s footer says the five are the same instruments as Home’s tape; they are not, and the panel says so — Home’s tape is the β benchmark plus the names carrying the most exposure, by that page’s own design rule, and rewriting either to match the other would break one of the two designs. Deeper event context points at /research/explorer?tab=events rather than /research/events, which is designed and unbuilt. And the day P&L the design puts in the Market streams header is already on this page as the STK STREAMS summary bar, with since and daily in both dollars and percent; a second total beside it would be the same figure printed twice.',
  '/research/event-radar':
    'Walked 2026-09-22 against Research Event Radar.dc.html at Rev 2026-09-20.16, rendered side by side with the app on local DEV (:5173). The design relabels this route **Alerts** — conditions you armed, and what has fired — and moves the 30-day calendar to /research/events. This side held an Event Radar: an events table, theme aggregates and a forward calendar. The split was already recorded in researchNavCatalog.ts, waiting for Events to exist before the calendar could come off; two measurements made it safe to do now. The events half has a home that is not this route — /research/explorer?tab=events renders the same EventRadarBody, and has since the Owner’s 2026-09-20 tab ruling. And the events half has nothing in it: all four stores answer zero rows on DEV 2026-09-22 — event-radar/events (also with include_dropped), events/calendar, events/themes and events/batches — with excluded_placeholder_rows 0, so nothing was filtered out either; the vendor gap behind them is a subscription one the Stock ratings walk already recorded. Nothing was deleted: the board moved file (EventRadarPage.tsx → EventRadarTab.tsx, its page export dropped, its one consumer repointed) and the header links to it. Built: the header and its Events calendar →, the Fired panel with its one-queue-two-views line, and the Armed panel with the D10 sentence the design puts in its header. Fired reads the queue the design names. Its footer says same items as the Analyze-alerts group — one queue, two views — so the page reads /research/alerts through the same three helpers the shell panel uses, which moved to lib/alertRanking when this page became their second reader (§14.2). Measured on DEV 2026-09-22, the whole store within its own caps (200 rows / 90 days): 44 alerts, weight_shift 23 and hit_rate_drop 21, over vrp 16 · gex_regime 10 · iv_rank 7 · momentum 7 · opex_pin 4, severity warn 38 and info 6 — and every one of them has symbol null. This queue is about lenses, not names, so Scope is the lens and the prototype’s per-symbol fixture rows (SMCI through a short-strike buffer) are the design’s illustration rather than a promise this store can keep. Two findings came out of it. The first is the header: the design reads N today, and today is almost always zero here — the newest alert is 2026-09-18 — so “0 today” would read as an all-clear, which is the one thing an alert surface may never say by accident. The line names the newest day the store has, how many fired on it, and that nothing has fired since. The second is a link that was already wrong: alertHref attached ?lens= to every hit_rate_drop and weight_shift row, and SignalDecayPage never read the parameter — it initialised its selector with useState and only read ?regime=. Every one of those links landed on IV Rank whatever lens it named. Fixed on both ends: the page reads ?lens= now, and the link only attaches one Signal Decay can select (momentum is 7 of the 44 and is not one of its six). The lens list moved to api/research/signalDecay as SIGNAL_DECAY_LENSES, because LENS_OPTIONS and DECAY_LENSES were two copies of the same six and a deep link may only name a lens the destination can show. Armed is owed, and every source for it was checked before saying so. /research/alerts holds only what has fired. /research/playbook/rules carries a title, a category and prose — no symbol, no predicate — the same shape the Stock ratings walk found. /research/playbook/triggers needs a symbol and answers satisfied true on all 42 rows it returns for NVDA: a log of triggers already satisfied, never a pending one, though its condition_snapshot does carry a real prob-against-threshold pair. And the risk limit book has the design’s exact shape — a reading, a line, the distance — while being a different object: portfolio lines that Limits & Breaches owns, not conditions armed from a symbol. The section keeps its place and names all four rather than drawing an empty table that would read as nothing is armed. Diverged, each with its reason. The design says the Analyze-alerts group belongs to the Inbox; on this side it belongs to the Alerts panel, because the shell’s own ruling (_Shell StatusBar.dc.html) is that Alerts holds messages and Inbox holds work, and two counts called Inbox on one screen was the collision that ruling ended. The page reads 90 days where the panel reads 14 — a page holds the record, a panel holds what is live — and the header says so rather than leaving two counts to disagree silently. The since column is the alert’s own movement (its two weekly rates, or its 30-day rate against its own mean) rather than the design’s price drift, which has no symbol to price. Events calendar → points at /research/explorer?tab=events rather than /research/events, which is designed and unbuilt; the parameter is one that page reads. And the fired-alert Inspector with its Trigger context section is owed: there is no inspector for an alert on this side, and the row’s destination is the lens page that can show the same history. Owner signed off 2026-09-23 on local DEV (:5173) at this rev.',
  '/home':
    'Walked 2026-09-17 against Home Today.dc.html at page rev 2026-09-17.1, on local DEV (:5173). The design\u2019s own note is the brief: Today belongs to no layer and cuts across all five by time of day, every row is a thing to DO with a deep link into the layer that owns the figure, and a row that is only a number would make it a board \u2014 \u201ca board is what fails at 15:45 with pin risk on three positions\u201d. Built as fourteen checks rather than a row list, because a check has three outcomes the page must keep apart: rows (yes, and here is what to do), clean (asked, nothing there), and cannot-run (nothing on this side can ask it, and why). That split is a deliberate deviation from the prototype\u2019s empty state, which asserts \u201cNo expiries today, no overnight stop breach, capital unchanged\u201d \u2014 three clean answers in one sentence, two of which this book cannot give. Seven checks run and seven cannot, and every one of the seven says which half is missing: no daily snapshot, so no overnight capital move (B1); no plan has ever been linked to a position, so neither the stop nor the planned exit can be watched; no future earnings date reaches this side; nothing records that a trade was reviewed; and the research scan the candidate check would read is not built. The seven that run are expiry inside two days, a corporate action landing on an open leg, limits over their line, a plan intended and unfilled, a strike pinning into the close, a short leg in the money, and fills booked today. Each reads the page that owns it through the same shared hook that page uses \u2014 useRiskExposure, useAssignmentLegs, the Limits model \u2014 so a row and its destination cannot disagree (\u00a714.2). Deliberate calls: the design gives each of the five layers its own hex; those five colours are not in this palette, so each maps to the token that already means that layer here rather than admitting five new colours for one page. A Blind spots panel states the thing a quiet action surface must state \u2014 absence of a row is not absence of a problem \u2014 and says how many questions could be asked at all. Re-laid out the same day after the Owner compared it with the prototype and found it too far off: the first pass had crammed the clock, the account chips and the due counts into the header, flattened each row onto one line, left out the item counts, and repeated a paragraph of caveats under every segment, which made a page about what to do read as a page about what cannot be read. Now built as the design has it \u2014 a NOW strip of its own (clock in New York\u2019s zone, session name, due / soon / later, date and account scope), the ambient Tape, per-segment item counts, and a two-line row: layer colour square, urgency, the thing to do, the detail under it, and the layer plus the page it opens on the right. The caveats moved into the one Blind spots panel, which now lists every unaskable question with its reason and its link, so the rows are the page and the caveats stay findable. The Tape is built to the design\u2019s own selection rule \u2014 the benchmark \u03b2 is measured against, then the names carrying the most exposure, each with a note saying why it is there \u2014 and outside the session, when the quote cache is empty, the row prints no price and the panel says so rather than showing a stale last as though it were live. One correctness fix came out of the walk: after the closing bell the live window is the next open, not Pre-close, so the page no longer points the reader at a deadline that passed hours ago. The app opens here: the Owner moved the index off /research on 2026-09-17, so / lands on Today. The target is one exported constant (INDEX_ROUTE) with a test on it, because a front door that quietly moves is the kind of change nobody notices until they go looking for the page that used to be first. Owner to look before aligned.',
}
