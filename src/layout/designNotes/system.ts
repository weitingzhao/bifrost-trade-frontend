/**
 * Design-walk notes · System.
 *
 * System and Docs — the machine room, and the pages about the app itself.
 *
 * One file per layer because the record only grows: the notes are long on
 * purpose, and a single catalogue crossed the 800-line ratchet on the day the
 * Review re-walk was written. Split by the group each page belongs to, which
 * is the seam the reader already has in their head.
 */
export const SYSTEM_NOTES: Record<string, string> = {
  '/research/lens-coverage':
    'Walked 2026-09-22 against Research Lens Coverage.dc.html at Rev 2026-09-19.2, rendered side by side with the app on local DEV (:5173). The design makes exactly one change to this page and its own footer says why — the old page showed one bar per lens across the whole universe; this splits it by tier because the Book being 100% and Extended being 40% are different problems — and this side was the old page. The split costs nothing: GET /research/screen/coverage already takes a tiers filter, so a column is the same call with one tier in it. Built, in the prototype’s order: the four-cell strip, the matrix of lenses × tiers with its Blocked by column and its footer, the Unscreenable panel, the Reach bars, Calibration → and ↻ Re-read universe. Measured before it was built (DEV 2026-09-22), and the split earns itself on the first row it draws. Universe 647 — resident 22 · core 577 · edge 48 — and IV Rank reads 99% of core against 71% of the edge, while one number over everything says 97% and hides both. The four per-tier calls are real readings, not shares of the whole wearing a tier’s name: no_option_face is 0 · 3 · 7 and sums to the 10 the all-tier call reports. The finding is the page’s headline zero. every_face is 0 in every tier, which reads as a coverage disaster; it is one lens — order_sentiment returns 0 of 647 while the other nine screenable lenses run 88–100%, so no symbol can read all ten. The strip now carries that sentence under it, because printing the zero alone sends the reader hunting a systemic failure that is not there. Two lenses are unscreenable with the engine’s own reasons (skew needs a 252-day percentile of atm_slope; term_slope is derived from two dte rows per symbol with no dte-based reader yet) and they render grey with the reason beside them rather than red — the design is explicit that grey is not a failure. Extracted before duplicating (§14.2): coverageLamp and coveragePct moved out of the page into coverageModel.ts before the matrix became their second reader, which is also what kept the duplicate-name ratchet flat. Owed, and marked in place rather than drawn dead. Δ 7d: nothing on this side stores yesterday’s coverage, so there is no previous reading to difference against — named in the matrix footer rather than drawn as a column of twelve dashes, which is the only divergence from the prototype’s table. The click-a-cell Missing aside keeps its place and says why it is empty: the response carries counts, not names, so a cell click would open a panel with nothing in it; it needs the endpoint to return the unread symbols per lens, and until then the matrix cells are deliberately not clickable rather than clickable into nothing. With the names would come the design’s fixes — Subscribe chain, Backfill, Drop from tier — which are collector writes and belong to the Ops side of that seam in any case. Diverged, each with its reason. The tier labels are the engine’s (resident · core · edge), not the design’s Book · Watch · Core · Extended: those words name the option universe’s own tiers, the reader learns them one click away on the Universe page, and renaming them here would make the page disagree with the store it reads. A fourth all column is kept beside the three tiers because the face summaries and the strip are read over the whole universe, and a summary whose number appears nowhere in its own table is the failure this page exists to prevent. The prototype’s as-of stamp is not drawn: the response carries no asof and the page would have to stamp its own clock, which is the thing Signal Health is ground truth against.',
  '/research/signal-health':
    'Walked 2026-09-22 against Research Signal Health.dc.html at Rev 2026-09-19.2, rendered side by side with the app on local DEV (:5173). The design calls this page ground truth for every asof in the console (§17) — every AsofTag reads this page’s asof, never its own clock — and it earns that with one line beside the Overall tag: not degraded, but which lens is late and what that does to the readings downstream of it. The page had the tag and the timestamp and not the line, which is the half a reader can act on. Built: the Overall rule line (and the judged-by), Expected and Downstream on the freshness table, the design’s legend about grey rows, the two compositions, Universe readiness, and the Lens Coverage → the design puts in the header. The finding is two headlines that hide their own composition, measured on DEV 2026-09-22. Canonical P&L answers insufficient_pct 0.0 — every leg priced — and by_quality says all 234,415 rows are iv_interpolated: not one priced off a full chain, which is what the design’s 96.4% means. 0% insufficient is true and reads as the opposite. IV reconstruction answers solver_ok_pct 0.997 while by_status says 1,950,684 of 2,130,014 rows are vendor_snapshot — 91.6% never reach the solver at all; it succeeded at what it was asked, and it was asked about 8% of the data. Neither number is wrong and neither is worth much without the breakdown under it, so both panels lead with the breakdown and say what the headline does not. The second finding is smaller and worse: both blocks carry an error field the page never read. On DEV the IV block answers rows 0 with error canceling statement due to statement timeout — a Postgres timeout on that one query — and the page rendered 0 rows · Solver OK —, which reads as the solver produced nothing rather than this did not run. A failed block now says so and its numbers are called absent, not zero. Universe readiness comes from the SEPA criteria stats, which the design notes this page absorbed from the stock screener: universe 5,321 · tech-ready 4,539 (85%) · fund-ready 3,522 (66%) · no fundamentals 1,799. Its own sentence is carried too, because it is the important half — readiness is a coverage fact, not a stock pick. Owed: the design’s Both · enters ratings row. The criteria stats count the fundamental and technical sides independently and carry no intersection, so a number there would be an assumption about overlap; the row shows and says owed. Diverged, each with its reason. Downstream links only where this side has the page — an approximate destination answers where does this land with a guess — and the four tables with no reader render plain. The design lists eight lenses and this engine probes six; the six are its own (vrp, canonical_pnl, iv_reconstructed, playbook_trigger, scan, forecast_settlement), so the table is the engine’s rather than the design’s names over different tables. The design’s unprobed status has no row today because this engine probes everything it lists; the legend still names it, because an unprobed lens is not a down lens is a rule about reading a grey row, not a row that must exist. And the Hypotheses panel stopped printing active twice — total_active beside a counts map that already contains it said 32 twice. Contract: sla_hours added to SignalFreshnessItem as optional — the engine sends it per row and the type did not carry it, so the Expected column had nothing to read; a row without one says no cadence recorded rather than being judged against a number invented here.',
  '/system/status':
    'Built 2026-09-22 against System Status.dc.html at Rev 2026-09-20.16 — the app had no page here. It is the Owner’s 2026-09-15 ruling made into a page: System collapses to this and Settings. The design’s own footer draws the line the page keeps — green means trade, amber means trade with the stated caveat, red means stop, and anything needing a graph, a log or a rerun button is the Ops Console’s job — so there are three panels, one per question a trader asks before opening a position, and no controls at all. Built: the header with its one line, the three domain panels (lamp · name · state · one sentence of why · the business page that owns the detail · in Ops), the per-panel detail bullets, and the footer carrying the ruling. Nothing here is computed for this page, which is the point — Trading link is the monitor’s own health.block_reasons and daemon heartbeat, Market data is computeMarketStreamsLamp and countFreshQuotes (what Live’s own header reads), Nightly data is overallRule, which is Signal Health’s own sentence. A status page with an opinion of its own can disagree with the page it sends you to, and then the reader has two answers and no way to choose (§14.2). Measured on DEV 2026-09-22, and the page earned itself on the first day: health.block_reasons answers [ib_not_connected, socket_massive_disconnected] while daemon.heartbeat.daemon_alive is true, so Trading link renders red and names both in words rather than as log strings — positions still read, a new one does not go out. Market data read all 22 subscribed streams current, and Nightly data read all six lenses within cadence with canonical_pnl oldest at 23.0h against a 36h SLA. Diverged, with its reason. The design’s footer says the top-bar system lamp and this page read the same three aggregates; they do not yet. The bar’s lamp summarises the platform plugins, which is a fourth question — is the control plane up — and not one a trader asks before opening a position. worstLamp is the value the bar would read if it changed; what the bar watches is a shell decision with a cost the design does not price (three aggregates on every page rather than one), so it is named here rather than made. Settings is written plain rather than linked: the design’s Settings page is not built, and /settings today redirects to the Coverage page the same ruling retired — an approximate destination is worse than none. The nine /system/* pages keep their rows and their “to ask” question; this page is their collapse target, not their deletion, and where each capability lands is the Owner’s call.',
  '/settings':
    'Built 2026-09-22 against Settings.dc.html at Rev 2026-09-20.16 — the app had no page here, and /settings was a redirect to the Coverage page the same ruling retired. It is the other half of the Owner’s 2026-09-15 collapse: System becomes Status and this. The prototype’s footer sets the boundary — the old System › Configuration › IB Connection merges in here, and cluster, pipeline and market-data infrastructure config belongs to the Ops Console — so there are three panels and nothing about the cluster on any of them. Built: IB Connection with the design’s three rows (User · Client ID · Account) and its own header count, Flex with Flex Query, Flex Preference, Range and a real Fetch now, and Keyboard. Read here, written where the write already lives. The prototype is read-only and says so — 此页原型只读，编辑动作在实现侧接 YAML/配置存储 — and this side already has that implementation on the IB Connection page, so each row reads its value and Edit → opens the page that owns the write. Moving 545 lines of write paths is not a presentation change; the design’s merge is recorded and the move is the Owner’s to call. The secret rule is kept literally: the page reports that a Flex token is set and its last four, which is what the plugin itself reports, and never the token. Keyboard is the app’s own table rather than the design’s four lines. SHORTCUTS already answers ? in the Omnibar, and retyping the design’s list would have made two lists that can disagree — publishing it turned up two ways the one table was already wrong: Esc was described as closing the Copilot, which stopped being true when §5a.8 made the side panel the companion that stays (it closes the top inspector, then the open float, and the panel answers to neither), and ⌘B — which @bifrost/ui’s sidebar has always bound — was missing entirely. Both fixed at the source with tests (§14.2). Measured on DEV 2026-09-22: the monitor’s config.ib_client carries the client hosts, port types and the three account fields; the Flex plugin’s /flex/config/summary answers with both tokens set and their last four, range 30d with a 270d first run, and /flex/coverage/freshness answers flex-trades and flex-transactions both landed within the hour. Owed, with its reason. The design’s Flex header reads daily 08:30 · missed today, ran 06:02 — a schedule and a comparison against it. The plugin exposes neither: its schedule is Dagster’s and no route in its OpenAPI reports it, so the header says what did land and when, which is the half that can be read, and turns amber once a daily pull has plainly missed one.',
  '/system/coverage':
    'Removed by design Rev 2026-09-15.13. Ingest and coverage detail is Ops Console work; this console keeps only the nightly-data lamp on /system/status.',
  '/system/feed':
    'Removed by design Rev 2026-09-15.13. Feed diagnosis moves to the Ops Console; the trade-facing half is the market-data row on /system/status.',
  '/system/data-readiness':
    'Removed by design Rev 2026-09-15.13. "Did the data land" is answered by Signal Health and the nightly-data row on /system/status.',
  '/system/topology':
    'Removed by design Rev 2026-09-15.13 — cluster topology is Ops Console material, not a trading question.',
  '/system/daemon':
    'Removed by design Rev 2026-09-15.13. "Can I trade" survives as the trading row on /system/status; the daemon detail is Ops.',
  '/system/api':
    'Removed by design Rev 2026-09-15.13 — API latency is diagnosis, and diagnosis is the Ops Console.',
  '/system/socket':
    'Removed by design Rev 2026-09-15.13 — socket state is Ops Console material; the trader sees the market-data lamp instead.',
  '/system/platform':
    'Removed by design Rev 2026-09-15.13 — plugin scheduling belongs to the Ops Console.',
  '/system/ib':
    'Absorbed by design Rev 2026-09-15.13 into `/settings` (IB user, client id, account) — trader-owned configuration, not a system view.',
}
