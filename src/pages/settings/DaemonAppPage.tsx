export default function DaemonAppPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Daemon App</h1>
      <p className="text-muted-foreground mt-1">Runtime config (heartbeat interval, etc.)</p>
    </div>
  )
}
