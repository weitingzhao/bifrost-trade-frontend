import { EmptyState } from '@/components/data-display'

/** Option coverage body — full migration pending (CI build stub). */
export function OptionCoverageBody() {
  return (
    <EmptyState title="Option coverage" description="Option pipeline coverage migration in progress." />
  )
}
