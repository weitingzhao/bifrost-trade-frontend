import { EmptyState } from '@/components/data-display'

/** Coverage overview summary — full migration pending (CI build stub). */
export function CoverageOverviewSummaryBody() {
  return (
    <EmptyState
      title="Coverage overview"
      description="Summary metrics migration in progress. Use Legacy Frontend for full coverage until this page is completed."
    />
  )
}
