import { EmptyState } from '@/components/data-display'

/** Per-symbol coverage detail — full migration pending (CI build stub). */
export function DataOverviewDetailBody() {
  return (
    <EmptyState title="Coverage detail" description="Per-symbol coverage migration in progress." />
  )
}
