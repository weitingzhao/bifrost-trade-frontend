import { EmptyState } from '@/components/data-display'

/** IB live stock coverage — full migration pending (CI build stub). */
export function StockIbCoverageBody() {
  return (
    <EmptyState title="Stock coverage (IB)" description="IB live bar coverage migration in progress." />
  )
}
